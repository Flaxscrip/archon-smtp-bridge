import { IPFSClient } from './types.js';
interface KuboClientConfig {
    url: string;
    waitUntilReady?: boolean;
    intervalSeconds?: number;
    chatty?: boolean;
    becomeChattyAfter?: number;
    maxRetries?: number;
}
interface PeeringPeer {
    ID: string;
    Addrs: string[];
}
declare class KuboClient implements IPFSClient {
    private ipfs;
    static create(options: KuboClientConfig): Promise<KuboClient>;
    connect(options: KuboClientConfig): Promise<void>;
    waitUntilReady(options: KuboClientConfig): Promise<void>;
    isReady(): Promise<boolean>;
    addText(text: string): Promise<string>;
    getText(cid: string): Promise<string>;
    addData(data: Buffer): Promise<string>;
    getData(cid: string): Promise<Buffer>;
    addDataStream(stream: AsyncIterable<Uint8Array>): Promise<string>;
    getDataStream(cid: string): AsyncIterable<Uint8Array>;
    addJSON(json: any): Promise<string>;
    getJSON(cid: string): Promise<any>;
    getID(): Promise<any>;
    getPeerID(): Promise<string>;
    getAddresses(): Promise<string[]>;
    addPeer(peer: string): Promise<boolean>;
    addPeers(peers: string[]): Promise<any>;
    getPeers(): Promise<any>;
    readonly PeeringPeers = "Peering.Peers";
    addPeeringPeer(id: string, addresses: string[]): Promise<void>;
    removePeeringPeer(id: string): Promise<void>;
    getPeeringPeers(): Promise<PeeringPeer[]>;
    resetPeeringPeers(): Promise<void>;
}
export default KuboClient;
