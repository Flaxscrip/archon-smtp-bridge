import { StoredWallet } from '../types.js';
import { AbstractBase } from './abstract-base.js';
export default class WalletMongo extends AbstractBase {
    private client;
    private db?;
    private collection?;
    private dbName;
    private readonly collectionName;
    static create(): Promise<WalletMongo>;
    constructor(walletKey?: string);
    connect(): Promise<void>;
    disconnect(): Promise<void>;
    saveWallet(wallet: Exclude<StoredWallet, null>, overwrite?: boolean): Promise<boolean>;
    loadWallet(): Promise<StoredWallet | null>;
}
