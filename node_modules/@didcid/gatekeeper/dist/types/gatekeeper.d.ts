import { BatchMetadata, BlockId, BlockInfo, CheckDIDsOptions, GatekeeperInterface, GatekeeperEvent, GatekeeperOptions, ImportEventsResult, Operation, DidCidDocument, ResolveDIDOptions, GetDIDOptions, CheckDIDsResult, ImportBatchResult, ProcessEventsResult, VerifyDbResult, Proof } from './types.js';
declare enum ImportStatus {
    ADDED = "added",
    MERGED = "merged",
    REJECTED = "rejected",
    DEFERRED = "deferred"
}
export default class Gatekeeper implements GatekeeperInterface {
    private db;
    private eventsQueue;
    private readonly eventsSeen;
    private verifiedDIDs;
    private isProcessingEvents;
    private ipfs;
    private cipher;
    readonly didPrefix: string;
    private readonly maxOpBytes;
    private readonly maxQueueSize;
    supportedRegistries: string[];
    private didLocks;
    private searchIndex;
    constructor(options: GatekeeperOptions);
    initSearchIndex(): Promise<void>;
    private updateSearchIndex;
    private withDidLock;
    verifyDb(options?: {
        chatty?: boolean;
    }): Promise<VerifyDbResult>;
    checkDIDs(options?: CheckDIDsOptions): Promise<CheckDIDsResult>;
    listRegistries(): Promise<string[]>;
    resetDb(): Promise<boolean>;
    generateCID(operation: unknown, save?: boolean): Promise<string>;
    generateDID(operation: Operation): Promise<string>;
    verifyOperation(operation: Operation): Promise<boolean>;
    verifyDIDFormat(did: string): boolean;
    verifyDateFormat(time?: string): boolean;
    verifyHashFormat(hash: string): boolean;
    verifyProofFormat(proof?: Proof): boolean;
    verifyCreateOperation(operation: Operation): Promise<boolean>;
    verifyUpdateOperation(operation: Operation, doc: DidCidDocument): Promise<boolean>;
    queueOperation(registry: string, operation: Operation): Promise<void>;
    createDID(operation: Operation): Promise<string>;
    generateDoc(anchor: Operation, defaultDID?: string): Promise<DidCidDocument>;
    resolveDID(did?: string, options?: ResolveDIDOptions): Promise<DidCidDocument>;
    updateDID(operation: Operation): Promise<boolean>;
    deleteDID(operation: Operation): Promise<boolean>;
    getDIDs(options?: GetDIDOptions): Promise<string[] | DidCidDocument[]>;
    exportDID(did: string): Promise<GatekeeperEvent[]>;
    exportDIDs(dids?: string[]): Promise<GatekeeperEvent[][]>;
    importDIDs(dids: GatekeeperEvent[][]): Promise<ImportBatchResult>;
    removeDIDs(dids: string[]): Promise<boolean>;
    importEvent(event: GatekeeperEvent): Promise<ImportStatus>;
    importEvents(): Promise<ImportEventsResult>;
    processEvents(): Promise<ProcessEventsResult>;
    verifyEvent(event: GatekeeperEvent): Promise<boolean>;
    importBatch(batch: GatekeeperEvent[]): Promise<ImportBatchResult>;
    importBatchByCids(cids: string[], metadata: BatchMetadata): Promise<ImportBatchResult>;
    exportBatch(dids?: string[]): Promise<GatekeeperEvent[]>;
    getQueue(registry: string): Promise<Operation[]>;
    clearQueue(registry: string, events: Operation[]): Promise<boolean>;
    getBlock(registry: string, block?: BlockId): Promise<BlockInfo | null>;
    addBlock(registry: string, block: BlockInfo): Promise<boolean>;
    addText(text: string): Promise<string>;
    getText(cid: string): Promise<string | null>;
    addData(data: Buffer): Promise<string>;
    getData(cid: string): Promise<Buffer | null>;
    addDataStream(stream: AsyncIterable<Uint8Array>): Promise<string>;
    getDataStream(cid: string): AsyncIterable<Uint8Array>;
    addJSON(json: object): Promise<string>;
    getJSON(cid: string): Promise<object | null>;
    searchDocs(q: string): Promise<string[]>;
    queryDocs(where: Record<string, unknown>): Promise<string[]>;
    search(query: {
        where: Record<string, unknown>;
    }): Promise<string[]>;
}
export {};
