import { ArchonError } from '@didcid/common/errors';
import { IPFSClient } from './types.js';
interface HeliaConfig {
    minimal?: boolean;
    datadir?: string;
}
export declare class NotConnectedError extends ArchonError {
    static type: string;
    constructor();
}
declare class HeliaClient implements IPFSClient {
    private config;
    private helia;
    private ipfs;
    private unixfs;
    constructor(config?: {});
    start(): Promise<void>;
    stop(): Promise<void>;
    addJSON(data: any): Promise<string>;
    getJSON(b58cid: string): Promise<any>;
    addText(data: string): Promise<string>;
    getText(b58cid: string): Promise<string>;
    addData(data: Buffer): Promise<string>;
    getData(b58cid: string): Promise<Buffer>;
    addDataStream(stream: AsyncIterable<Uint8Array>): Promise<string>;
    getDataStream(b58cid: string): AsyncIterable<Uint8Array>;
    static create(config?: HeliaConfig): Promise<HeliaClient>;
}
export default HeliaClient;
