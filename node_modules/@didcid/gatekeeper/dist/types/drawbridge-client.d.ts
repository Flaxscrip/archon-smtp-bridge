import { DrawbridgeInterface, GatekeeperClientOptions, LightningBalance, LightningConfig, LightningInvoice, LightningPayment, LightningPaymentRecord, LightningPaymentStatus } from './types.js';
import GatekeeperClient from './gatekeeper-client.js';
export default class DrawbridgeClient extends GatekeeperClient implements DrawbridgeInterface {
    static create(options: GatekeeperClientOptions): Promise<DrawbridgeClient>;
    isLightningSupported(): Promise<boolean>;
    createLightningWallet(name: string): Promise<LightningConfig>;
    getLightningBalance(invoiceKey: string): Promise<LightningBalance>;
    createLightningInvoice(invoiceKey: string, amount: number, memo: string): Promise<LightningInvoice>;
    payLightningInvoice(adminKey: string, bolt11: string): Promise<LightningPayment>;
    checkLightningPayment(invoiceKey: string, paymentHash: string): Promise<LightningPaymentStatus>;
    publishLightning(did: string, invoiceKey: string): Promise<{
        ok: boolean;
        publicHost?: string;
    }>;
    unpublishLightning(did: string): Promise<boolean>;
    getLightningPayments(adminKey: string): Promise<LightningPaymentRecord[]>;
    zapLightning(adminKey: string, did: string, amount: number, memo?: string): Promise<LightningPayment>;
}
