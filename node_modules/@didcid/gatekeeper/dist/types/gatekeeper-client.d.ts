import { type AxiosInstance } from 'axios';
import { BatchMetadata, BlockId, BlockInfo, GatekeeperClientOptions, GatekeeperInterface, GatekeeperEvent, GetStatusResult, Operation, DidCidDocument, ResolveDIDOptions, GetDIDOptions, ImportBatchResult, ProcessEventsResult, VerifyDbResult } from './types.js';
export default class GatekeeperClient implements GatekeeperInterface {
    protected API: string;
    protected baseUrl: string;
    protected axios: AxiosInstance;
    static create(options: GatekeeperClientOptions): Promise<GatekeeperClient>;
    constructor();
    addCustomHeader(header: string, value: string): void;
    removeCustomHeader(header: string): void;
    get url(): string;
    connect(options?: GatekeeperClientOptions): Promise<void>;
    waitUntilReady(options: GatekeeperClientOptions): Promise<void>;
    listRegistries(): Promise<string[]>;
    resetDb(): Promise<boolean>;
    verifyDb(): Promise<VerifyDbResult>;
    isReady(): Promise<boolean>;
    getVersion(): Promise<{
        version: string;
        commit: string;
    }>;
    getStatus(): Promise<GetStatusResult>;
    createDID(operation: Operation): Promise<string>;
    resolveDID(did: string, options?: ResolveDIDOptions): Promise<DidCidDocument>;
    updateDID(operation: Operation): Promise<boolean>;
    deleteDID(operation: Operation): Promise<boolean>;
    getDIDs(options?: GetDIDOptions): Promise<string[] | DidCidDocument[]>;
    exportDIDs(dids?: string[]): Promise<GatekeeperEvent[][]>;
    importDIDs(dids: GatekeeperEvent[][]): Promise<ImportBatchResult>;
    exportBatch(dids?: string[]): Promise<GatekeeperEvent[]>;
    importBatch(batch: GatekeeperEvent[]): Promise<ImportBatchResult>;
    importBatchByCids(cids: string[], metadata: BatchMetadata): Promise<ImportBatchResult>;
    removeDIDs(dids: string[]): Promise<boolean>;
    getQueue(registry: string): Promise<Operation[]>;
    clearQueue(registry: string, events: Operation[]): Promise<boolean>;
    processEvents(): Promise<ProcessEventsResult>;
    addJSON(data: object): Promise<string>;
    getJSON(cid: string): Promise<object>;
    addText(data: string): Promise<string>;
    getText(cid: string): Promise<string>;
    addData(data: Buffer): Promise<string>;
    getData(cid: string): Promise<Buffer>;
    addDataStream(stream: AsyncIterable<Uint8Array>): Promise<string>;
    getDataStream(cid: string): AsyncGenerator<Uint8Array>;
    getBlock(registry: string, block?: BlockId): Promise<BlockInfo | null>;
    addBlock(registry: string, block: BlockInfo): Promise<boolean>;
    generateDID(operation: Operation): Promise<string>;
    searchDocs(q: string): Promise<string[]>;
    queryDocs(where: Record<string, unknown>): Promise<string[]>;
    search(query: {
        where: Record<string, unknown>;
    }): Promise<string[]>;
}
