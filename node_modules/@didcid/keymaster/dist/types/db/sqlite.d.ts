import { StoredWallet } from '../types.js';
import { AbstractBase } from './abstract-base.js';
export default class WalletSQLite extends AbstractBase {
    private readonly walletName;
    private db;
    static create(walletFileName?: string, dataFolder?: string): Promise<WalletSQLite>;
    constructor(walletFileName?: string, dataFolder?: string);
    connect(): Promise<void>;
    disconnect(): Promise<void>;
    saveWallet(wallet: StoredWallet, overwrite?: boolean): Promise<boolean>;
    loadWallet(): Promise<StoredWallet | null>;
}
