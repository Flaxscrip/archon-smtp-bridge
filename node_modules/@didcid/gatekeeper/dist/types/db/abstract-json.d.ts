import { BlockId, BlockInfo, JsonDbFile, GatekeeperDb, GatekeeperEvent, Operation } from '../types.js';
export declare abstract class AbstractJson implements GatekeeperDb {
    protected readonly dataFolder: string;
    protected readonly dbName: string;
    private _lock;
    private runExclusive;
    protected constructor(name: string, folder?: string);
    protected abstract loadDb(): JsonDbFile;
    protected abstract writeDb(db: JsonDbFile): void;
    abstract resetDb(): Promise<void | number | JsonDbFile>;
    start(): Promise<void>;
    stop(): Promise<void>;
    private splitSuffix;
    addEvent(did: string, event: GatekeeperEvent): Promise<void>;
    getEvents(did: string): Promise<GatekeeperEvent[]>;
    setEvents(did: string, events: GatekeeperEvent[]): Promise<void>;
    deleteEvents(did: string): Promise<void>;
    queueOperation(registry: string, op: Operation): Promise<number>;
    getQueue(registry: string): Promise<Operation[]>;
    clearQueue(registry: string, batch: Operation[]): Promise<boolean>;
    getAllKeys(): Promise<string[]>;
    addBlock(registry: string, blockInfo: BlockInfo): Promise<boolean>;
    getBlock(registry: string, blockId?: BlockId): Promise<BlockInfo | null>;
    addOperation(opid: string, op: Operation): Promise<void>;
    getOperation(opid: string): Promise<Operation | null>;
}
