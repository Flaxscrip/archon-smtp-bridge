import type { StoredWallet, WalletBase } from '../types.js';
export declare abstract class AbstractBase implements WalletBase {
    private _lock;
    abstract saveWallet(wallet: StoredWallet, overwrite?: boolean): Promise<boolean>;
    abstract loadWallet(): Promise<StoredWallet | null>;
    updateWallet(mutator: (wallet: StoredWallet) => void | Promise<void>): Promise<void>;
}
